[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m intel.manage init-db && python -m intel.manage ingest && uvicorn intel.api:app --host 0.0.0.0 --port $PORT --workers 1"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
