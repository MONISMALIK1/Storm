# Intelligence pipeline dependencies (in addition to your existing scrapers).
# Install with: pip install -r requirements-pipeline.txt

# Web framework
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9     # form data for /auth/login

# Auth
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7.4
email-validator>=2.1        # pydantic EmailStr

# Data
SQLAlchemy>=2.0
pydantic>=2.5

# Sentiment (optional — falls back to a lexicon if missing)
vaderSentiment>=3.3.2
